---
cr: {{CR}}
base: {{BASE}}                     # 侦察基点 commit; §1 的 file:line 相对它成立
created: {{DATE}}
updated: {{DATE}}
---

# {{CR}} 实施 Spec: {{TITLE}}

<!-- 本文回答 "改哪里, 按什么顺序改, 怎么测, 怎么上线". 业务规则不在这里重写, 引用
     REQ / CR 条目号. 一句话目标写在下面, 让读者三行内知道这是干什么的. -->

一句话目标:

---

## 0. 口径演化 (可选)

<!-- 需求口径在实施期间变过就留档: 初稿 -> 收窄 -> 恢复, 谁在什么日期决定, 现行口径见
     CR 哪条. 没变过就删掉本节. -->

## 1. 代码侦察结论 (改动依据)

<!-- 整份 spec 的发动机. 只写**核实过的现状事实**, 每条 file:line (相对基点), 并说明这个
     事实如何约束改动 -- "X 是唯一入口, 所以改 X 一处即可"; "Y 的 CHECK 硬性要求 Z, 所以
     不能伪装成旧终局, 必须新终局 + 迁移". 不写打算怎么做 (那在 §2-§4).
     为什么: AI 最不可靠的恰恰是 "以为自己理解了代码". 把侦察钉成文本, 后续每个会话 /
     每个代理都站在同一份事实上, 也让 review 有东西可核.
     典型要核实的: 唯一入口 / 调用点, 约束与触发器, 幂等键与锁的范围, 队列的进入条件
     (会不会堵队首), 配置阶梯的先例, 迁移号与配套守卫, 测试夹具的位置. -->

1. **<事实标题>**: <事实, file:line> -- <约束了什么改动>.
2.

## 2. 改动总览 (按包)

| 包 / 文件 | 类型 | 内容 |
|---|---|---|
| | 新增 / 修改 | |

**明确不改**: <!-- 列出刻意不动的包与原因, 防止顺手扩散. -->

## 3. 数据与迁移

<!-- 迁移号, up 要点 (非逐字), down 的拒绝条件 (终局是审计记录时 down 必须失败),
     回填与否, 能否先行独立合并, 配套守卫 (lock / DBML / 迁移测试). 无迁移写 "无". -->

## 4. 分步实施顺序 (每步独立可合并, 可回退)

<!-- 每步的约束是 "合并后系统仍然正确" -- 迁移先行且零行为变化, 开关默认关时全路径等价,
     切流单独一步. 落点列: 完成填提交 hash, 未做填 **待办**, 不填勾 (提交可验证, 勾不可).
     sdd.py status 靠这一列判断进度, 请保持四列表头. -->

| 步 | 内容 | 合并即生效? | 落点 |
|---|---|---|---|
| 1 | | 否 (行为无变化) | 待办 |
| 2 | | | 待办 |

## 5. 测试计划与验证

<!-- 编号 T1..Tn, 每条标注对应的 AC / BR, 覆盖 CR 变更内容表里每一条 AC. 说明真库还是单元,
     夹具在哪. 并发 / 幂等类断言要写清两个连接还是同一事务 (同一事务里第二次锁是空操作).
     迁移往返 (up -> down -> up) 也算一条. -->

- **T1** (AC-x):

### 上线前核查

<!-- 发布闸门: 存量核查 SQL / precheck 脚本, 退出码语义, 谁签认. -->

### 上线后验证

## 6. 风险与回退

<!-- 每条: 风险 -> 缓解 / 回退动作. 回退是开关还是迁移 down, 回退会不会销毁数据. -->

## 7. 明确不做 (本次范围外)

<!-- 每条带理由或去向 ("另立 CR", "现状缺陷 REQ-x BR-y, 本次不修"). 这是对实施者约束力
     最强的一节之一 -- 顺手多改是最常见的越界. -->

## 8. 上线前硬性检查单

<!-- 逐项可执行: 工单, 关闭条件, 责任人, 部署不变量 (单实例? 整体替换?), 盘点查询. -->

1.

## 9. 实施偏差与补充事实

<!-- 计划与现实的对账, 实施中追加: 哪步与 §4 不同及原因; 侦察漏掉的事实; 未随提交合入
     的东西及原因; 本机跑不了由 CI 覆盖的守卫; 编号变动 (迁移号重排等). 这一节让
     后来者不用重新考古. -->
