-- {{CR}} 手工 SQL: {{NAME}}
--
-- 这份文件是 "要人手在数据库上跑的那一段 SQL" 的**唯一**来源: 跑的人整段复制它, 不从
-- release/CR-NNN-<slug>.md 或 PR 正文里抄描述. 只有跑完就没用的 SQL 才放这里 --
-- 以后新环境还要用的属于迁移或项目里入库的 seed 脚本 (四类归宿见 skill 的
-- references/conventions.md "手工 SQL: 先判归宿, 再决定放哪").

-- 归宿: 一次性回填 / 数据修复 (跑完即弃)
-- 不进迁移的理由: <只对当前存量成立 / 值按环境不同 / 要分批与监控, 不能卡住 migrator>
-- 跑的时机: <迁移之后? 滚动容器之前? 开关打开之前?>
-- 幂等: <是, 靠 ON CONFLICT / WHERE 收窄; 跑第二遍会 ...>
-- 预估影响行数: <数字, 从下面的核对查询 (1) 得来>
-- 回退: <怎么撤; 撤不了写 "不可撤: 原因">
--
-- 执行记录 (跑过才填; env= 还是空的时候 sdd.py prune 不许删这份文件):
--   env=            date=            rows=            by=

-- (1) 跑之前核对: 应该影响多少行 (填进上面的预估行数)
SELECT count(*) FROM <表> WHERE <条件>;

-- (2) 正文. 幂等写法; 大批量分批跑 (每批 N 行 + 间隔), 别一条语句锁住整张表.
BEGIN;

-- INSERT INTO <表> (...) VALUES (...) ON CONFLICT (<幂等键>) DO NOTHING;
-- UPDATE <表> SET ... WHERE <只命中该改的行>;

COMMIT;

-- (3) 跑之后核对: 期望看到什么 (待处理行数归零, 状态分布, 金额合计不变)
SELECT count(*) FROM <表> WHERE <条件>;   -- 期望: 0
