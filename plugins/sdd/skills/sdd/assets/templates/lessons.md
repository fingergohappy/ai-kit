# 错题本

<!-- 从已 fixed 的 review 提炼出来的可复用教训. 入库, 长期保存; review 所在的工作目录提炼后整个删除.
     一行一条, 编号 L-n 永不复用. 只收 "下次还会犯" 的模式, 不收一次性笔误.
     同一模式再犯时不加新行: 在原行 "来源" 追加, "次数" +1 -- 次数是最有用的信号, 它告诉 review
     该把哪些问题从 "会查" 升级成 "必查" (review-lanes: 次数 >= 2 必查).
     车道: A 完整性/文档  B 金融安全/正确性  C 合规/安全  D 代码设计  E 测试  R 审查方 (review 自己的误报 / 漏报).
     写法: 模式 = 犯了什么类型的错, 泛化到本 CR 之外 (不写具体函数名);
           识别信号 = 看到什么就该警觉 (能当 grep 关键词或审查问题用);
           规则 = 以后怎么做, 一句可执行的话;
           来源 = CR-NNN 与发现编号 (review 删除后仍能追到 CR 第 5 节的摘要). -->

| 编号 | 车道 | 模式 | 识别信号 | 规则 | 来源 | 次数 |
|---|---|---|---|---|---|---|
